const app=document.getElementById('app');
async function load(){try{const r=await fetch('/api/system');if(r.status===401){app.innerHTML='<section class="connect"><div class="mark">IP</div><h1>InfraPilot</h1><p>Device not authenticated.</p><p class="muted">Run <code>infrapilot sk create</code>, then pair it with<br><code>infrapilot sk replace &lt;token&gt;</code>.</p></section>';return}const system=await r.json();app.innerHTML=`<div class="dashboard"><header class="top"><div><div class="eyebrow">Infrastructure control plane</div><h1>InfraPilot</h1></div><span class="status">Connected</span></header><section class="grid"><article class="card"><h2>System</h2><div class="value">${system.os||'Unknown'}</div><p class="muted">${system.kernel||''}</p></article><article class="card"><h2>Compute</h2><div class="value">${system.cpus||'--'} CPUs</div><p class="muted">Memory and load available through the Agent</p></article><article class="card"><h2>Services</h2><div class="value status">Agent</div><p class="muted">Web Panel connected</p></article><article class="card"><h2>Modules</h2><div class="empty">No modules installed</div></article></section></div>`}catch(e){document.getElementById('message').textContent='Unable to connect'}}load();
