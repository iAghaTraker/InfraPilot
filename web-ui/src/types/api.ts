export type Discovery = { available: boolean; device_id?: string };
export type SystemInfo = { os?: string; kernel?: string; cpus?: number; memory?: string; disk?: string; uptime?: string };
export type Session = { session: string };
