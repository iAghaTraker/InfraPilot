import { useCallback, useEffect, useState } from 'react'; import { getSystem } from '../api/system'; import type { SystemInfo } from '../types/api'; import { Layout } from '../components/Layout'; import { MetricCard } from '../components/MetricCard'; import { Panel } from '../components/Panel'; import { Loading } from '../components/Loading'; import { ErrorState } from '../components/ErrorState'; import { EmptyState } from '../components/EmptyState'; import { StatusBadge } from '../components/StatusBadge';
export function Dashboard(){const [system,setSystem]=useState<SystemInfo>();const [error,setError]=useState('');const load=useCallback(()=>{setError('');setSystem(undefined);getSystem().then(setSystem).catch(()=>setError('The Agent did not return system information.'));},[]);useEffect(load,[load]);if(error)return <Layout><ErrorState message={error} retry={load}/></Layout>;if(!system)return <Layout><Loading/></Layout>;return <Layout><section className="welcome"><div><p className="eyebrow">System overview</p><h2>Good morning</h2><p className="muted">A clear view of your local infrastructure.</p></div><StatusBadge label="All systems operational"/></section><section className="metric-grid"><MetricCard label="CPU cores" value={String(system.cpus||'--')} detail="Available to Agent" icon="CPU"/><MetricCard label="Memory" value={system.memory||'--'} detail="Current host capacity" icon="RAM"/><MetricCard label="Disk" value={system.disk||'--'} detail="Primary volume" icon="DSK"/><MetricCard label="Uptime" value={system.uptime||'--'} detail="Since last restart" icon="UP"/></section><section className="dashboard-grid"><Panel title="System"><div className="details"><div><span className="muted">Operating system</span><strong>{system.os||'Unknown'}</strong></div><div><span className="muted">Kernel</span><strong>{system.kernel||'Unknown'}</strong></div><div><span className="muted">Architecture</span><strong>{(system as SystemInfo & {architecture?:string}).architecture||'--'}</strong></div></div></Panel><Panel title="Services"><div className="service-row"><div><strong>InfraPilot Agent</strong><p className="muted">Control plane</p></div><StatusBadge label="Running"/></div><div className="service-row"><div><strong>Web Panel</strong><p className="muted">Local interface</p></div><StatusBadge label="Connected"/></div></Panel><Panel title="Connection"><div className="details"><div><span className="muted">Authentication</span><strong>Ed25519 device identity</strong></div><div><span className="muted">Session</span><strong>Active · 15 minutes</strong></div></div></Panel><Panel title="Modules"><EmptyState title="No modules installed" detail="Optional infrastructure modules will appear here when available."/></Panel></section></Layout>}
