import type { ReactNode } from 'react'; import { StatusBadge } from './StatusBadge'; import { logout } from '../api/auth';
export function Layout({ children }: { children: ReactNode }) { const signOut=()=>logout().then(()=>location.reload()); return <div className="app-layout"><aside className="sidebar"><div className="brand-row"><span className="brand-mark">IP</span><strong>InfraPilot</strong></div><nav><a className="active" href="#dashboard">Dashboard</a><a href="#servers">Servers</a><a href="#modules">Modules</a><a href="#services">Services</a><a href="#settings">Settings</a></nav><div className="sidebar-foot"><span className="muted">Local control plane</span><StatusBadge label="Secure"/></div></aside><div className="content"><header className="topbar"><div><span className="eyebrow">Operations overview</span><h1>Dashboard</h1></div><div className="top-actions"><StatusBadge label="Connected"/><span className="device-label">Device authenticated</span><button className="ghost" onClick={signOut}>Log out</button></div></header>{children}</div></div>; }
